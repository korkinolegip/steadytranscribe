# Состояние проекта

## Последнее обновление
2026-07-08 — v1.5.0. Большой UX-раунд по обратной связи Олега: надёжная отмена, стабильный ETA + таймер на разделении, авто-очистка файла, обучаемая оценка времени, авто-обновление по умолчанию, гарантия «нет процессов-сирот», раздел «Как пользоваться» с анимацией, правки → словарь, статистика в минутах/словах.

## Что сделано
- [x] Ядро: ffmpeg → faster-whisper (turbo по умолчанию), офлайн, все причины вылета устранены
- [x] Оболочка как FluidVoice + брендинг SteadyControl (логотип на настоящем знаке SC)
- [x] Разделение по собеседникам (sherpa-onnx) — кнопкой на результате, в отдельном процессе, с указанием числа участников
- [x] Правки текста, переименование расшифровки, имена собеседников, история с поиском
- [x] Два установщика: лёгкий (222 МБ) и с моделью (1.57 ГБ, для флешки)
- [x] Автообновление ВНУТРИ программы (тихая per-user установка без UAC + автоперезапуск)
- [x] Прогресс с ETA (прошло/осталось), оценка времени при выборе файла, уведомление «файл готов» при свёрнутом окне
- [x] Авто-режим нагрузки (пониженный приоритет + регулировка по активности окна — без настроек)
- [x] Автоотправка логов разработчику (ntfy.sh + зеркало), кнопка Telegram
- [x] Полное удаление (вся папка + опционально модели/история), защита от процессов-сирот, freeze_support от fork-бомбы

## Новое в v1.5.0 (по обратной связи Олега)
- [x] Отмена работает реально: ffmpeg убивается принудительно при конвертации; разделение корректно останавливается; отмена — тихо, без красной ошибки
- [x] Стабильный ETA: обратный отсчёт от оценки с мягкой коррекцией (EMA) — не скачет вверх-вниз
- [x] Таймер прошло/осталось теперь и на «Разделить по собеседникам»
- [x] База времён обработки (`storage/timings.py`): учится на реальных запусках → оценка со временем точная именно для этого ПК
- [x] Авто-очистка выбранного файла после готовой расшифровки — можно сразу тащить следующий
- [x] Статистика в минутах + количество слов; неуверенные расшифровки помечаются «проверьте текст»
- [x] Авто-обновление ПО УМОЛЧАНИЮ: тихо качается в фоне, ставится при закрытии (тумблер в Настройках)
- [x] Гарантия «нет сирот»: все дочерние процессы в Windows Job Object с KILL_ON_JOB_CLOSE — умирают вместе с приложением даже при краше
- [x] Правки текста → новые имена/термины попадают в словарь автоматически
- [x] Раздел «Как пользоваться» — визуальный гид (3 шага, карточки функций, решение проблем) с плавным появлением

## Новое в v1.5.1 — обновления «как в Chrome» (по обратной связи Олега)
- [x] Staged-схема: тихо скачали → отложили на «полку» (updates/pending.json, переживает перезапуск и крах)
- [x] Установка в 3 момента: при ПРОСТОЕ (окно неактивно ≥10 мин, ничего не обрабатывается → тихо ставится, возвращается свёрнутой), при ВЫХОДЕ (без перезапуска, /NORELAUNCH), при СЛЕДУЮЩЕМ ЗАПУСКЕ (до открытия окна)
- [x] Во время расшифровки/разделения/скачивания моделей установка никогда не стартует
- [x] Защита от цикла: 2 неудачные попытки → отложенное обновление сбрасывается
- [x] Повторная проверка обновлений каждые 4 часа (программу держат открытой днями)
- [x] Кнопка «Проверить обновления» в Настройках теперь тоже ставит ВНУТРИ программы (раньше открывала браузер — жалоба Олега)
- [x] ЗАДЕЛ НА БУДУЩЕЕ (чтобы никогда не пришлось переустанавливать вручную):
  - [InstallDelete] сносит старый код (_internal, exe) перед установкой — не копятся устаревшие библиотеки (классическая причина сломанных обновлений PyInstaller); {app}\models сохраняется
  - settings_version + механизм миграций настроек (_MIGRATIONS)
  - Выбор установщика по каноничному имени SteadyTranscribe-Setup-X.Y.Z.exe с fallback
  - КОНТРАКТЫ (менять НЕЛЬЗЯ): AppId GUID, имя exe, папка установки {localappdata}\Programs\SteadyTranscribe, имя лёгкого установщика, лёгкий установщик публикуется в КАЖДОМ релизе

## Сейчас в работе
Сборка v1.5.1 в CI. Олег обновляется вручную на v1.5.0 (внутри программы) → дальше всё само.

## Следующий шаг
1. Олег тестит v1.5.0: отмена, ETA (не должен прыгать), таймер разделения, авто-очистка файла, раздел «Как пользоваться», авто-обновление
2. По итогам: качество разделения на реальных Zoom-встречах; при жалобах на скорость — модель поменьше
3. Возможно: платный сертификат подписи (убрать «неизвестный издатель» полностью) — обсудить, ~$100-400/год

## Известные проблемы
- «Неизвестный издатель» / SmartScreen при ПЕРВОЙ установке — убирается только платным code-signing сертификатом. При авто-обновлениях (per-user, тихо) окон нет.
- Диаризация ~3 мин на 11-мин файл (sherpa на CPU) — приемлемо, идёт в фоне
- Turbo на CPU медленная (~4-5 мин на 11 мин аудио) — объективно, это большая модель; small втрое быстрее но чуть ниже качество

## Ключевые уроки (диагностика вылетов на Windows — все через CI-эмуляцию реального exe)
- Вылет №1: не попадал silero_vad_v6.onnx (faster_whisper data) → collect_data_files
- Вылет №2: fork-бомба от py-cpuinfo (запускал копии exe) → ctypes IsProcessorFeaturePresent + freeze_support
- Вылет №3: device=cuda/auto лез на видеокарту (нет CUDA-библиотек) → форс device="cpu"
- Вылет №4: нехватка памяти при диаризации (turbo + процесс) → unload модели перед разделением
- Пунктуация терялась из-за словаря-подсказки через запятую → оформлять как предложение с точкой
- Обновление «не ставилось» — старая версия не заменялась (per-user + тихая установка решает)
- Настройки переживали переустановку (Windows не чистит %APPDATA%) → деинсталлятор чистит
